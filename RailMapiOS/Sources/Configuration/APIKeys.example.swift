//
//  APIKeys.example.swift
//  RailMapiOS
//
//  TEMPLATE — copy to `APIKeys.swift` and fill in. The real file is
//  git-ignored (see `.gitignore`).
//
//  The token must match one entry of the server's `API_AUTH_TOKENS` env var.
//  Rotation pattern:
//    1. Add a new token to the server's `API_AUTH_TOKENS` (comma-sep).
//    2. Ship a new iOS build with the new token here.
//    3. Remove the old token from the server.
//

/*
 enum APIKeys {
     static let railmapToken = "REPLACE_ME"
 }
 */
