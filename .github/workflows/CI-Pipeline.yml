name: CI Pipeline

on:
  pull_request:
    branches:
      - dev
  push:
    branches:
      - dev
      - main

jobs:
  lint:
    runs-on: macos-latest      
    steps:
      - uses: actions/checkout@v4
      - uses: cirruslabs/swiftlint-action@v1
        with:
          version: latest

  build-and-test:
    runs-on: macos-latest
    needs: lint
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - uses: swift-actions/setup-swift@v2
        with:
         swift-version: "6.0"

      - name: Verify Swift Version
        run: swift --version

      - name: Generate Xcode Project
        uses: jdx/mise-action@v2
        
      - name: Ensure Tuist Version is Pinned
        run: mise use --pin tuist
        
      - name: Generate Xcode Project
        run: tuist generate

      - name: Build Project
        run: xcodebuild build -project RailMapiOS.xcodeproj -scheme RailMapiOS -destination "platform=iOS Simulator,OS=18.0,name=iPhone 15 Pro" -configuration Debug

      - name: Run Tests
        run: xcodebuild test -project RailMapiOS.xcodeproj -scheme RailMapiOS -destination "platform=iOS Simulator,OS=18.0,name=iPhone 15 Pro" -configuration Debug
